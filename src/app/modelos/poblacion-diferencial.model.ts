export class PoblacionDiferencial {
    cod_poblacion: number;
    nom_poblacion: string;
    
    constructor(
        cod_poblacion: number,
        nom_poblacion: string,
    ){
        this.cod_poblacion = cod_poblacion;
        this.nom_poblacion = nom_poblacion;
    }
}
