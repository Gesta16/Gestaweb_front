export class Riesgo {

    cod_riesgo: number;
    nom_riesgo: string;

    constructor(
        cod_riesgo: number,
        nom_riesgo: string
    ) {
        this.cod_riesgo = cod_riesgo;
        this.nom_riesgo = nom_riesgo;
    }

}
