export class NumeroControl {
    cod_controles: number;
    num_control: number;

    constructor(
        cod_controles: number,
        num_control: number,
    ){
        this.cod_controles = cod_controles;
        this.num_control = num_control;
    }
}
