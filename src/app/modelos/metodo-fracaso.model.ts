export class MetodoFracaso {
    cod_fracaso: number;
    nom_metodo: string;
  
    constructor(cod_fracaso: number, nom_metodo: string) {
      this.cod_fracaso = cod_fracaso;
      this.nom_metodo = nom_metodo;
    }
  }
  