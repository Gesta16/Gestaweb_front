export class NumSesionesCurso {
    cod_sesiones: number;
    num_sesiones: number;

    constructor(
        cod_sesiones: number,
        num_sesiones: number,
    ){
        this.cod_sesiones = cod_sesiones;
        this.num_sesiones = num_sesiones;
    }
}
